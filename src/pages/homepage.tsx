import React from 'react';
import setTitle from '../utils/docsettings';

// could get the drawing for bid shirts somewhere

export default function Homepage() {
    setTitle("Home");

    return (
        <div className='homeDiv' style={{width:'100%', height:'100%'}}>
            
        </div>
    )
}